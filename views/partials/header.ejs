<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Project Playlist</title>
</head>
<body>
  <nav>
    <!-- <img src="Super wicked and relevent image url"> -->
    <a href="/" <%- title === 'All Playlists' ? 'class="active"' : '' %>>All Playlists</a>
    <% if (user) { %>
      <a href="/"	<%- title === 'New Playlist' ? 'class="active"' : '' %>>
        New Playlist</a>
      <a href="/" <%- title === 'Browse Songs' ? 'class="active"' : '' %>>Browse Songs</a>
      <a href="/logout">LOG OUT</a>
    <% } else { %>
      <a href="/auth/google" class="login">LOG IN&nbsp;<img style="height: 20px;" src="https://i.imgur.com/FHjYyi0.png"></a>
    <% } %>
  </nav>